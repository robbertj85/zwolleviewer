/**
 * groningen provincial layers.
 *
 * TODO: Investigate provincial open data endpoints:
 *   - https://geo.groningen.nl, https://services.groningen.nl
 * No confirmed public WFS/REST endpoints found yet -- stub in place.
 */

import type { CityConfig } from "../../cities";
import type { DataSource } from "../types";

export function buildGroningenLayers(_city: CityConfig): DataSource[] {
  return [];
}
